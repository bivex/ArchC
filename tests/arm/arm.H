/******************************************************
 * Architecture Module header file.                   *
 * This file is automatically generated by ArchC      *
 * WITHOUT WARRANTY OF ANY KIND, either express       *
 * or implied.                                        *
 * For more information on ArchC, please visit:       *
 * http://www.archc.org                               *
 *                                                    *
 * The ArchC Team                                     *
 * Computer Systems Laboratory (LSC)                  *
 * IC-UNICAMP                                         *
 * http://www.lsc.ic.unicamp.br                       *
 ******************************************************/
 

#ifndef  _ARM_H
#define  _ARM_H

#include "arm_parms.H"
#include "systemc.h"
#include "ac_module.H"
#include "ac_utils.H"
#include "arm_arch.H"
#include "arm_isa.H"
#include "arm_syscall.H"


class arm: public ac_module, public arm_arch {
private:
  typedef struct {
    uint8_t cond;
    uint8_t op;
    uint8_t cmd;
    uint8_t s;
    uint8_t rn;
    uint8_t rd;
    uint16_t op2;
  } T_Type_DP;

  typedef struct {
    uint8_t cond;
    uint8_t op;
    uint8_t i;
    uint8_t p;
    uint8_t u;
    uint8_t b;
    uint8_t w;
    uint8_t l;
    uint8_t rn;
    uint8_t rd;
    uint16_t offset;
  } T_Type_MEM;

  typedef struct {
    uint8_t cond;
    uint8_t op;
    uint8_t l;
    int32_t offset;
  } T_Type_BR;

  typedef struct {
    uint8_t cond;
    uint8_t op;
    uint32_t imm;
  } T_Type_HLT;

  typedef struct {
    bool valid;
    void* end_rot;
    unsigned id;
    union {
      T_Type_DP F_Type_DP;
      T_Type_MEM F_Type_MEM;
      T_Type_BR F_Type_BR;
      T_Type_HLT F_Type_HLT;
    };
  } DecCacheItem ;

public:

  bool has_delayed_load;
  char* delayed_load_program;
  arm_parms::arm_isa ISA;
  //!Address of Interpretation Routines.
  void** IntRoutine;

  DecCacheItem* DEC_CACHE;
  DecCacheItem* instr_dec;
  bool start_up;

  //!Dispatch Method.
  inline __attribute__((always_inline)) void* dispatch();

  //!Behavior execution method.
  void behavior();

  SC_HAS_PROCESS( arm );

  sc_event wake;

  //!Constructor.
  arm( sc_module_name name_ ): ac_module(name_), arm_arch(), ISA(*this) {
    SC_THREAD( behavior );
    sensitive << wake;
    has_delayed_load = false; 
    start_up=1;
    ac_id.write(globalId++);
  }

  void init_dec_cache() {
    DEC_CACHE = (DecCacheItem*) calloc(sizeof(DecCacheItem), (dec_cache_size / 4));
  }

  unsigned get_ac_pc();

  void set_ac_pc( unsigned int value );

  virtual void PrintStat();

  void init(int ac, char* av[]);

  void init();

  void set_prog_args();

  void load(char* program);

  void delayed_load(char* program);

  void stop(int status = 0);

  virtual ~arm() {};

};
#endif  //_ARM_H

